import React from "react";
import "@/app/globals-store-2.css";
const Store2Layout = ({ children }: { children: React.ReactNode }) => {
  return <div className="bg-yellow-100">{children}</div>;
};

export default Store2Layout;
